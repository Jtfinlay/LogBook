<div class="jumbo">
  <div class="right tomster"></div>
  <h2>Welcome!</h2>
  <p>We hope you find exactly what you're looking for in a place to stay.</p>
</div>

{{#each model as |timelog|}}
    <article>
        <h3>{{ timelog.createdAt }}</h3>
        <div class="detail">{{  format-datetime timelog.elapsedtime "MM:SS.sss" }}</div>
    </article>
{{/each}}